services:
  blog:
    build:
      context: .
      args:
        SITE_URL: ${SITE_URL:-https://example.com}
    image: personal-astro-blog:latest
    restart: unless-stopped
    ports:
      - "${BLOG_PORT:-8080}:80"
